[package]
name = "tong"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "A Rust-first hermetic build system with declarative manifests and extensible language rules."

[dependencies]

