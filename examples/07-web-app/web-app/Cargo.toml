[package]
name = "web-app"
version = "0.1.0"
edition = "2021"

[dependencies]
web-core = { path = "../web-core" }
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net"] }
